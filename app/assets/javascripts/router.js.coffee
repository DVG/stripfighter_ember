StripfighterEmber.Router.map ->
  @route "home", path: '/',
  @route "prompts", path: "/challenge",
  @route "registration", path: '/sign_up',
  @route "comics", path: '/comics'
  @route "login", path: '/login'
  @route "logout", path: '/logout'
